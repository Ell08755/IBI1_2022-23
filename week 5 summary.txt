#Week 5 summary
#notice: cause the summary is mainly written for me to understand better, may contain some chinese
several ways to print command in python
*print (a+b)
 a and b should all be string, without blank automatically
*print ('a',b)
 no need to all be string, automatically add blank
several calculation
a*=b means a=a*b, other calculation symbols the same
*import means you fetch a specific function from a library 从书架上拿书
e.g. ‘from random import randint’: this sentence means you fetch randint这个命令 from random这个命令库中, after this you can directly write randint(1,100)
*another way: import random|random.randint(1,100),要写random的前缀
*random：
 randint(a,b) fetch a random integer in [a,b)
 uniform(a,b) fetch a rational number in [a,b)
 random() fetch a random number [0,1)
 ^区间范围要求待判定，[)是否适用于所有？
*commit a remove command: git rm <filename>

